name: companion_app
description: Android companion app for Arduino Nano + HC-05 Bluetooth timer prompts.
publish_to: "none"

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bluetooth_serial:
    path: ../third_party/flutter_bluetooth_serial
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
